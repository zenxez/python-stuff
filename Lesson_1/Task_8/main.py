# Mixed Operations

pi = 3.14
circle_radius = float(input("Input circle radius: "))
circle_area = (circle_radius * circle_radius) * pi

print(f"If circle radius is {circle_radius} Circle area will be {circle_area}")